Newz 0.6.1

- Fixes broken/incomplete LootESP bundle integration.
- No LootESP module is required by Main or build.
- Adds optional Friend Dependencies loader for Stellar and sensoryESP.
- Remote dependencies are never auto-loaded. Use Settings > Friend Dependencies.
- Keeps NeverLose as the active Newz UI.
- Unknown FireServer path remains intentionally excluded until its purpose is proven.
