Newz 0.5.0 patch

ADDED
- src/Modules/Freecam.lua

UPDATED
- src/Config.lua
- src/Main.lua
- src/Ui.lua
- scripts/build.py
- scripts/check.py
- README.md

DELETE
- src/Modules/CorpseIllusion.lua
- REMOVE_OLD_CORPSE_ACTIONS.txt

FREECAM
- V: toggle by default
- WASD: move
- Space: up
- Ctrl: down
- Shift: boost
- Mouse: look
- keybind, speed, boost and sensitivity are configurable
- Teleport On Exit and Snap To Ground are configurable

The corpse visual/local preview has been removed completely.
Corpse ESP remains.

After replacing the files, run from the repository root:

powershell -ExecutionPolicy Bypass -File .\cleanup_old_files.ps1
python scripts/build.py
python scripts/check.py --require-dist
git status

Then, if everything is correct:

git add .
git commit -m "Add freecam and remove corpse preview"
git push origin main
