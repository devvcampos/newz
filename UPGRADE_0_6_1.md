# Newz 0.6.1

This package fixes the `LootESP.lua invalido` startup error caused by an incomplete bundle integration.

The optional loaders reconstructed from the friend's project are available under **Settings > Friend Dependencies**. Loading them does not replace the Newz NeverLose UI.

The unidentified `FireServer` path is still not included because its target/purpose has not been proven.
